REPLACE WITH: notes.png

Suggested shot: the desktop notes split-view (markdown editor on left,
rendered preview on right) with a note that uses several markdown
features — heading, list, a `[[Wikilink]]` to another note, maybe a
small code block. The folder/tag chips in the metadata row should be
visible.

Spec:
  - Format:    PNG, lossless or lightly compressed
  - Width:     ~1600 px
  - File size: ≤ 300 KB after optimisation

Delete this file once notes.png is in place.
