REPLACE WITH: mobile.png

Suggested shot: the mobile note view in portrait orientation (under
768px), so the full-screen layout, the title row with pin icon, the
metadata row with folder + tag chips, and the segmented Bearbeiten /
Vorschau control are all visible. Use a clean device frame (e.g. via
deviceframes.com) or a plain screenshot — both fine.

Spec:
  - Format:    PNG, lossless or lightly compressed
  - Width:     ~480 px (portrait — README inline embed is 40%)
  - File size: ≤ 200 KB after optimisation

Delete this file once mobile.png is in place.
